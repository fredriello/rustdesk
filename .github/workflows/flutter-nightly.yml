name: Flutter Nightly Build

# NOTE: GitHub Actions scheduled triggers only run on the repository's default
# branch. This workflow should exist on the `master` branch, and the
# repository's default branch should be set to `master` for the cron trigger
# to fire correctly.
on:
  schedule:
    # schedule build every night
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  sync-upstream:
    uses: ./.github/workflows/sync-upstream.yml
    secrets: inherit

  run-flutter-nightly-build:
    needs: [sync-upstream]
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
